<!-- brief pin; never edit by hand; re-record deliberately: SZ_RECORD_BRIEF_PINS=1 swift test --filter SZBriefPinTests -->
Nodes:
- `44444444-4444-4444-8444-444444444444` "Untitled" — prompt, no contract yet — prompt: (empty — the user has not described this node yet; do not invent its purpose)
- `55555555-5555-4555-8555-555555555555` "Levels" — generated (NEEDS REBUILD — its code predates the current contract), contract[in: input:texture; out: output:texture] — prompt: "Adjust levels."

Flow edges (drawing intent — realize each into typed data wiring; laying the data edge resolves the arrow; an end written `node.port` was dropped on that exact slot — wire that port, not another): none
Data edges: none
Render endpoint (blitted to the viewport): none